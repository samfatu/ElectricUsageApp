import { Device, DeviceListCalculateResult } from "../types";

export const calculateDevice = (device: Device) : Device | null => {

  return null;
}

export const calculateTotal = (deviceList: Device[]) : DeviceListCalculateResult | null => {

  return null;
}